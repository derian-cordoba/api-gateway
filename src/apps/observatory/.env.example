# Address of the gateway management API. This is used only on the Observatory server.
GATEWAY_MANAGEMENT_URL=http://localhost:3000/management
GATEWAY_MANAGEMENT_TOKEN=

# Polling is performed by the browser against Observatory's same-origin API.
OBSERVATORY_POLL_INTERVAL_MS=10000
